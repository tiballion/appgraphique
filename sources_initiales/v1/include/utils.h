#ifndef UTILS_H
#define UTILS_H
#include <string>
#include <vector>

void decouper(const std::string &str, std::vector<std::string> &tokens, const std::string &delimiters = " ");

#endif // UTILS_H